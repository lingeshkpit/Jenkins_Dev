pipeline {
    agent any
    environment {
    DOCKERHUB_CREDENTIALS = credentials('lingeswararao')
    }
    stages {
          stage ('MVN_build ') {
            steps {
                sh '''
                 rm -rf /var/lib/jenkins/workspace/maven_pipe/my-app
                mvn archetype:generate -DgroupId=com.mycompany.app -DartifactId=my-app -DarchetypeArtifactId=maven-archetype-quickstart -DarchetypeVersion=1.4 -DinteractiveMode=false
               '''
                }
             }
        stage('Docker_images') {
            steps {
                sh 'docker images'
            }
        }
    
        stage('Build docker image') {
            steps {
                sh 'docker build -t lingeswararao/tomcat .'
            }
        }
        
       stage('Push container') {
        steps {
            sh '''
              echo $DOCKERHUB_CREDENTIALS_PSW | docker login -u $DOCKERHUB_CREDENTIALS_USR --password-stdin
              docker tag tomcat lingeswararao/tomcat
              docker push lingeswararao/tomcat
             '''
           }
           }
         stage('Docker_RMI') {
            steps {
                sh 'docker rmi -f $(docker images -q)'
            }
          }
     }
      }
